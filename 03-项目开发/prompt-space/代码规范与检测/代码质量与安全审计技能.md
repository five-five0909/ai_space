# 🛠️ Claude Code Skill: 安全审计与防护 (通用版)

**适用场景**：任何 Java 项目的 SQL 注入、XSS 攻击、CSRF 防护、敏感信息泄露

---

## 1. 快速开始

**如果项目存在以下风险，立即使用此 Skill：**
- 用户输入直接拼接到 SQL 中
- 密码明文存储在数据库
- 敏感信息（日志、返回）未脱敏
- 接口无权限校验
- 缺乏输入验证

**执行指令：**
> 请执行 **安全审计技能**，检查并修复安全问题

---

## 2. 第一步：安全检测 (AI 自动执行)

### 2.1 检测项目类型

```bash
# 检测构建工具
ls -la pom.xml build.gradle 2>/dev/null

# 检测安全框架
grep -E "spring-security|shiro|spring-boot-starter-security" pom.xml 2>/dev/null

# 检测 ORM 框架
grep -E "mybatis|mybatis-plus|jpa|hibernate" pom.xml 2>/dev/null
```

### 2.2 检测配置文件

```bash
# 查找配置文件
find . -name "application*.yml" -o -name "application*.properties" | head -5
find . -name "*Security*.java" 2>/dev/null | head -10
find . -name "*Filter*.java" 2>/dev/null | head -10
```

---

## 3. 第二步：SQL 注入防护

### 3.1 检测风险代码

```bash
# 搜索潜在 SQL 注入风险
grep -rnE "\$\{.*\}|concat\s*\(|executeQuery.*\+.*parameter" --include="*.java" src/ | head -20

# 搜索原始 JDBC 使用
grep -rnE "createStatement|executeQuery.*\"select" --include="*.java" src/ | head -10
```

### 3.2 MyBatis-Plus 安全写法

```java
// ✅ 正确：使用参数化查询 (#{})
@Mapper
public interface UserMapper extends BaseMapper<User> {

    // LambdaQueryWrapper - 推荐
    default User findByUsername(String username) {
        LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(User::getUsername, username);
        return selectOne(wrapper);
    }

    // Mapper XML - 使用 #{}
    @Select("SELECT * FROM users WHERE username = #{username} AND status = 1")
    User selectByUsername(@Param("username") String username);
}

// ✅ 正确：QueryWrapper 使用链式调用
List<User> users = userMapper.selectList(new LambdaQueryWrapper<User>()
    .eq(User::getStatus, 1)
    .like(User::getUsername, "john")
    .orderByDesc(User::getCreateTime)
    .last("LIMIT 10"));
```

```xml
<!-- ✅ 正确：MyBatis XML 使用 #{} -->
<select id="findByCondition" parameterType="map" resultType="User">
    SELECT id, username, email, status
    FROM users
    WHERE status = #{status}
    <if test="username != null and username != ''">
        AND username = #{username}
    </if>
    ORDER BY create_time DESC
</select>
```

```java
// ❌ 错误：使用 ${} 拼接（禁止）
@Select("SELECT * FROM users WHERE username = '${username}'")
User selectByUsernameDanger(@Param("username") String username);

// ❌ 错误：拼接 order by
@Select("SELECT * FROM users ORDER BY ${columnName}")
List<User> selectOrderBy(@Param("columnName") String columnName);

// ✅ 正确：白名单验证 + #{}
@Select("SELECT * FROM users ORDER BY #{columnName}")
List<User> selectOrderBySafe(@Param("columnName") String columnName);
```

### 3.3 JPA/Hibernate 安全写法

```java
// ✅ 正确：使用参数化查询
@Query("SELECT u FROM User u WHERE u.username = :username AND u.status = 1")
User findByUsername(@Param("username") String username);

// ✅ 正确：使用 Specification
Specification<User> spec = Specification.where(null);
if (username != null) {
    spec = spec.and((root, query, cb) ->
        cb.equal(root.get("username"), username));
}
List<User> users = userRepository.findAll(spec);
```

### 3.4 原始 JDBC 安全写法

```java
// ✅ 正确：使用 PreparedStatement
String sql = "SELECT * FROM users WHERE username = ? AND status = ?";
try (PreparedStatement pstmt = connection.prepareStatement(sql)) {
    pstmt.setString(1, username);
    pstmt.setInt(2, status);
    ResultSet rs = pstmt.executeQuery();
}

// ❌ 错误：使用 Statement 拼接
try (Statement stmt = connection.createStatement()) {
    String sql = "SELECT * FROM users WHERE username = '" + username + "'";
    ResultSet rs = stmt.executeQuery(sql);
}
```

---

## 4. 第三步：密码安全

### 4.1 检测风险

```bash
# 搜索明文密码存储
grep -rn "password.*=" --include="*.java" src/ | grep -v "password\s*=" | grep -v "setPassword\|getPassword" | head -10
```

### 4.2 安全密码存储

```java
// PasswordEncoder.java - 通用密码加密工具
package com.yourproject.utils;

import java.security.MessageDigest;
import java.security.SecureRandom;
import java.util.Base64;

public class PasswordEncoder {

    // 推荐：使用 BCrypt
    public static String bcryptEncode(String rawPassword) {
        // 需要添加依赖: spring-boot-starter-security
        return org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder.class
            .getConstructor(int.class, SecureRandom.class)
            .newInstance(10, new SecureRandom())
            .encode(rawPassword);
    }

    public static boolean bcryptMatch(String rawPassword, String encodedPassword) {
        return org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder.class
            .getConstructor(int.class, SecureRandom.class)
            .newInstance(10, new SecureRandom())
            .matches(rawPassword, encodedPassword);
    }

    // 备选：使用盐值 + SHA-256
    public static String sha256Encode(String rawPassword, String salt) {
        try {
            MessageDigest md = MessageDigest.getInstance("SHA-256");
            md.update(salt.getBytes());
            byte[] hashed = md.digest(rawPassword.getBytes());
            return Base64.getEncoder().encodeToString(hashed);
        } catch (Exception e) {
            throw new RuntimeException("密码加密失败", e);
        }
    }

    public static String generateSalt() {
        SecureRandom random = new SecureRandom();
        byte[] salt = new byte[16];
        random.nextBytes(salt);
        return Base64.getEncoder().encodeToString(salt);
    }
}
```

```java
// 用户注册
public void register(User user) {
    // 加密密码
    user.setPassword(PasswordEncoder.bcryptEncode(user.getPassword()));
    userRepository.save(user);
}

// 用户登录
public boolean login(String username, String password) {
    User user = userRepository.findByUsername(username);
    if (user != null && PasswordEncoder.bcryptMatch(password, user.getPassword())) {
        return true;
    }
    return false;
}
```

---

## 5. 第四步：XSS 防护

### 5.1 检测风险

```bash
# 搜索 HTML 输出
grep -rn "innerHTML|th:utext|\${" --include="*.html" --include="*.vue" src/ | head -10
```

### 5.2 输入过滤

```java
// XssUtils.java - 通用 XSS 过滤工具
package com.yourproject.utils;

import java.util.regex.Pattern;

public class XssUtils {

    private static final Pattern[] DANGEROUS_PATTERNS = {
        Pattern.compile("<script[^>]*>", Pattern.CASE_INSENSITIVE),
        Pattern.compile("javascript:", Pattern.CASE_INSENSITIVE),
        Pattern.compile("on\\w+\\s*=", Pattern.CASE_INSENSITIVE),
        Pattern.compile("<iframe[^>]*>", Pattern.CASE_INSENSITIVE),
        Pattern.compile("alert\\s*\\(", Pattern.CASE_INSENSITIVE),
        Pattern.compile("eval\\s*\\(", Pattern.CASE_INSENSITIVE),
    };

    public static String escapeHtml(String input) {
        if (input == null || input.isEmpty()) {
            return input;
        }
        return input
            .replace("&", "&amp;")
            .replace("<", "&lt;")
            .replace(">", "&gt;")
            .replace("\"", "&quot;")
            .replace("'", "&#x27;")
            .replace("/", "&#x2F;");
    }

    public static String stripDangerousContent(String input) {
        if (input == null || input.isEmpty()) {
            return input;
        }
        String result = input;
        for (Pattern pattern : DANGEROUS_PATTERNS) {
            result = pattern.matcher(result).replaceAll("");
        }
        return result;
    }
}
```

### 5.3 Spring XSS 过滤

```java
// XssFilter.java
package com.yourproject.filter;

import jakarta.servlet.*;
import jakarta.servlet.http.*;
import java.io.IOException;

public class XssFilter implements Filter {

    @Override
    public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain)
            throws IOException, ServletException {
        chain.doFilter(new XssHttpServletRequestWrapper((HttpServletRequest) request), response);
    }
}

// XssHttpServletRequestWrapper.java
package com.yourproject.wrapper;

import jakarta.servlet.http.*;
import java.util.regex.Pattern;

public class XssHttpServletRequestWrapper extends HttpServletRequestWrapper {

    private static final Pattern[] DANGEROUS_PATTERNS = {
        Pattern.compile("<script[^>]*>", Pattern.CASE_INSENSITIVE),
        Pattern.compile("javascript:", Pattern.CASE_INSENSITIVE),
        Pattern.compile("on\\w+\\s*=", Pattern.CASE_INSENSITIVE),
    };

    public XssHttpServletRequestWrapper(HttpServletRequest request) {
        super(request);
    }

    @Override
    public String getParameter(String name) {
        String value = super.getParameter(name);
        return cleanXss(value);
    }

    @Override
    public String[] getParameterValues(String name) {
        String[] values = super.getParameterValues(name);
        if (values == null) return null;
        return java.util.Arrays.stream(values).map(this::cleanXss).toArray(String[]::new);
    }

    private String cleanXss(String value) {
        if (value == null || value.isEmpty()) return value;
        String result = value;
        for (Pattern pattern : DANGEROUS_PATTERNS) {
            result = pattern.matcher(result).replaceAll("");
        }
        return result;
    }
}
```

### 5.4 Vue 模板安全

```vue
<!-- ✅ 正确：自动转义 -->
<div>{{ userContent }}</div>

<!-- ✅ 正确：谨慎使用 v-html -->
<div v-html="safeContent"></div>

<!-- ❌ 危险：用户输入直接使用 v-html -->
<div v-html="userInput"></div>
```

---

## 6. 第五步：敏感信息脱敏

### 6.1 日志脱敏

```java
// ✅ 正确：日志中脱敏敏感信息
@Slf4j
@Service
public class UserService {

    public void login(LoginRequest request) {
        // ❌ 错误：日志打印密码
        log.info("用户登录: {}", request);

        // ✅ 正确：日志脱敏
        log.info("用户登录: username={}, password=***", request.getUsername());
    }
}
```

### 6.2 返回值脱敏

```java
// UserVO.java
@Data
public class UserVO {

    private Long id;
    private String username;

    @JsonIgnore  // 不返回密码
    private String password;

    private String phone;

    // 手机号脱敏: 138****0000
    public String getPhone() {
        if (this.phone != null && this.phone.length() == 11) {
            return this.phone.substring(0, 3) + "****" + this.phone.substring(7);
        }
        return this.phone;
    }

    // 邮箱脱敏: j***@example.com
    public String getEmail() {
        if (this.email != null && this.email.contains("@")) {
            int atIndex = this.email.indexOf("@");
            if (atIndex > 2) {
                return this.email.substring(0, 2) + "***" + this.email.substring(atIndex);
            }
        }
        return this.email;
    }

    // 身份证脱敏
    public String getIdCard() {
        if (this.idCard != null && this.idCard.length() == 18) {
            return this.idCard.substring(0, 4) + "**********" + this.idCard.substring(14);
        }
        return this.idCard;
    }
}
```

---

## 7. 第六步：接口权限控制

### 7.1 通用权限注解

```java
// RequirePermission.java
package com.yourproject.annotation;

import java.lang.annotation.*;

@Target({ElementType.METHOD, ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
public @interface RequirePermission {
    String[] value() default {};
}

// PermissionAspect.java
@Aspect
@Component
public class PermissionAspect {

    @Autowired
    private UserDetailsService userDetailsService;

    @Around("@annotation(requirePermission)")
    public Object checkPermission(ProceedingJoinPoint point, RequirePermission requirePermission) throws Throwable {
        // 获取当前用户
        Authentication auth = SecurityContextHolder.getContext().getAuthentication();
        if (auth == null || !auth.isAuthenticated()) {
            throw new UnauthorizedException("未登录");
        }

        // 检查权限
        String[] required = requirePermission.value();
        // ... 权限检查逻辑

        return point.proceed();
    }
}
```

---

## 8. 第七步：验证清单

### 8.1 SQL 注入检查
- [ ] 所有 SQL 使用参数化查询 (#{})
- [ ] 禁止使用 ${} 拼接
- [ ] 禁止使用 Statement 执行原始 SQL

### 8.2 密码安全检查
- [ ] 密码使用 BCrypt 或等效加密
- [ ] 不在代码中硬编码密码
- [ ] 密码不在日志中输出

### 8.3 XSS 防护检查
- [ ] 用户输入经过滤
- [ ] HTML 输出经过转义
- [ ] 谨慎使用 v-html

### 8.4 敏感信息检查
- [ ] 日志中无敏感信息
- [ ] API 返回值经过脱敏
- [ ] 配置文件中的敏感信息加密

### 8.5 权限检查
- [ ] 所有 API 有权限控制
- [ ] 越权访问被拒绝
- [ ] 角色权限分配合理

---

## 9. 附录：安全依赖

```xml
<!-- Spring Security (推荐) -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>

<!-- Hutool (工具类) -->
<dependency>
    <groupId>cn.hutool</groupId>
    <artifactId>hutool-all</artifactId>
    <version>5.8.26</version>
</dependency>
```

---

## 执行指令

> 请执行 **安全审计技能**，按照以下步骤操作：
> 1. 检测项目安全配置
> 2. 检查 SQL 注入风险
> 3. 检查密码存储安全
> 4. 检查 XSS 防护
> 5. 检查敏感信息脱敏
> 6. 检查权限控制

---

## AI 执行要点

作为 AI，你需要：

1. **先检测后修改**：使用 grep 搜索风险代码
2. **提供修复代码**：让用户可以直接使用
3. **说明风险等级**：高/中/低
4. **提供最佳实践**：如使用 PreparedStatement、BCrypt
5. **考虑业务场景**：不要过度防护影响用户体验
